{
  "name": "@zaivim/vim-adapter",
  "version": "0.1.4",
  "private": false,
  "description": "zai.vim Vim adapter — VimScript glue layer + Python fallback shim",
  "scripts": {
    "lint": "vint autoload plugin || true"
  }
}
