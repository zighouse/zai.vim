[
  {
    "type": "function",
    "category": "os",
    "is_read_only": true,
    "is_concurrency_safe": true,
    "max_result_size": 2000,
    "prompt": "当需要获取系统信息（日期时间、语言环境、操作系统版本等）时使用",
    "function": {
      "name": "get_os_info",
      "description": "提供系统相关的信息获取功能，包括日期时间、地理位置、系统配置等",
      "parameters": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "description": "要执行的操作类型",
            "enum": [
              "get_date",
              "get_locale",
              "get_os_version"
            ]
          },
          "format": {
            "type": "string",
            "description": "日期格式（仅适用于 get_date 操作）",
            "enum": ["date_only", "datetime", "utc", "timestamp", "lunar", "all"],
            "default": "all"
          }
        },
        "required": ["action"]
      }
    }
  }
]
